# NOTE: torch is CPU-only here.  In Docker we install it separately from
# https://download.pytorch.org/whl/cpu before this file is read so the CUDA
# wheels are NEVER pulled.  For local installs run:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
# BEFORE `pip install -r requirements.txt`.

# API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7

# Configuration
python-dotenv>=1.0

# File typing & PDF metadata
filetype>=1.2
pypdf>=4.0

# Primary parser (multi-format + OCR + tables + layout) — MIT
docling>=2.0

# Secondary parsers — MIT
pdfplumber>=0.11
pdfminer.six>=20240706

# Office/HTML auxiliary adapters — MIT/Apache
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
trafilatura>=1.12
beautifulsoup4>=4.12

# Zero-shot NER — Apache-2.0
gliner>=0.2.13

# LLM extraction — Apache-2.0
instructor>=1.5
openai>=1.40

# Utilities
python-dateutil>=2.9
